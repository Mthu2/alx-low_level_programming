#include "main.h"
/**
 * _strcat - function name
 * @seat: parameter one
 * @src: parameter two
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	int destlen = 0;
	int srclen = 0;
	int i;

	for (i = o; dest[i] != '\0'; i++)
		detlen++;
	for (i = 0; src[i] != '\0'; i++)
		srclen++;

	for (i = 0; i <= srclen; i++)
	{
		dest[deslen + i] = src [i];
	}
	return (dest);
}
